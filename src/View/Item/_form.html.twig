<form method="post">
    <input type="hidden" value="{{ item.id }}" name="id">
    <input type="text" value="{{ item.title }}" name="title" placeholder="name">
    <input type="submit" value="Valider">
</form>
