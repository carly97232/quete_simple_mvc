{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Artist {{ item.title }}</h1>
    <p>{{ item.id }}. {{ item.title }}. {{ item.local }}</p>
    <a href="/item/edit/{{ item.id }}">Edit</a>
    <a href="/item/delete/{{item.id}}">Supprimer</a>
    <a href="/">Back</a>
{% endblock %}
